// a module is “a collection of Go packages stored in a file tree
// with a go.mod file at its root"
module ch11/goModTest/myProg

// When we add a package main to a module, we make it a program
// that can be compiled and executed.

go 1.18
